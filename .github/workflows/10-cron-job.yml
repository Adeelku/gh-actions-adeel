# Daily Cron Job
name: Daily Cron Job

on:
  schedule:
    - cron: '0 19 * * *'  

jobs:
  example_job:
    runs-on: ubuntu-latest
    steps:
      - name: Run a sample step
        run: echo "This job runs daily at 19:00 UTC."