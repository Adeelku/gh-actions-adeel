name: learn-github-actions

run-name: ${{ github.actor }} is showing GitHub Actions

on: [workflow_dispatch]

jobs:
  check-bats-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
